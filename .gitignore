# ignore node_modules which should be generated using `npm install` on the command
# line.
node_modules/

# artefacts from the Grunt build process generated using `grunt` on the command
# line.
dist/

# ignore the cache directory created by Sass compiler for quicker compilation.
.sass-cache/

# CSS files are generated by compiling Sass using `grunt sass`.
*.css
